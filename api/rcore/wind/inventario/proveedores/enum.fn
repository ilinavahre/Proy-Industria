
(sentinel::privilege-required "Administrador, Inventario")

(db::table `
    SELECT
        x.supplier_id id,
        x.name label

    FROM ##suppliers x
    WHERE x.is_active=1
    ORDER BY x.name ASC
`)
