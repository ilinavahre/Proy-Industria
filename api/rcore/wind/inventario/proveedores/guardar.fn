
(sentinel::privilege-required "Administrador, Inventario")

(shield::validate
    (shield::field id
        required true
        pattern integer
        check:record_not_found (isnotnull (db::scalar `SELECT supplier_id FROM ##suppliers WHERE is_active=1 AND supplier_id={$}`))
    )
)

(shield::validate input

    (shield::field name
        required false
        pattern text
        check:record_exists (isnull (db::scalar `SELECT supplier_id FROM ##suppliers WHERE supplier_id!={id} AND is_active=1 AND name={!$}`))
    )

    (shield::field is_visible
        required false
        pattern bit
    )

    (shield::field city
        required false
        pattern text
    )
    (shield::field state
        required false
        pattern text
    )

    (shield::field email
        required false
        pattern email
    )
    (shield::field phone
        required false
        pattern phone
    )
)

(db::update "##suppliers" "supplier_id=(id)" (input))
(&)
